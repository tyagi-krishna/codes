#include <stdio.h>
#include <math.h>

int main() {
    double x = 1000.0;
    printf("natural log of %.1f is %.4f. \n", x, log(x));
    printf("log10 of %.1f is %.4f. \n", x, log10(x));
    return 0;
}